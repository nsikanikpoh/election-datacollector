<%= form_for([@conversation]) do |form| %>
<form>

  <div class="field">
    <%= form.text_area :body, id: :conversation_body, placeholder: "Enter your text", required:"required", rows:10 %>
  </div>
 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</form>