<%= form_with(model: affiliation, local: true) do |form| %>
  <% if affiliation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(affiliation.errors.count, "error") %> prohibited this affiliation from being saved:</h2>

      <ul>
      <% affiliation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :affiliate_id %>
    <%= form.number_field :affiliate_id, id: :affiliation_affiliate_id %>
  </div>

  <div class="field">
    <%= form.label :referred_id %>
    <%= form.number_field :referred_id, id: :affiliation_referred_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
